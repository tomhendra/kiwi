import React, { FC } from 'react';
import { Button } from 'antd';
import './App.less';

const App: FC = () => (
  <div className="App">
    <Button type="primary">Click me</Button>
  </div>
);

export default App;
