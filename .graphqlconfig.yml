projects:
  buho:
    schemaPath: amplify/backend/api/buho/build/schema.graphql
    includes:
      - src/core/graphql/**/*.ts
    excludes:
      - ./amplify/**
    extensions:
      amplify:
        codeGenTarget: typescript
        generatedFileName: src/core/models/api.ts
        docsFilePath: src/core/graphql
extensions:
  amplify:
    version: 3
